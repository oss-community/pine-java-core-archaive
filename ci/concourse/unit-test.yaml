---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: maven
    tag: 3.8.6-openjdk-18

inputs:
  - name: repo

outputs:
  - name: repo

run:
  path: sh
  args:
    - -exc
    - |
      mvn -f repo/pom.xml test
